	<%= form_for ([@podcast, @episod]) do |f| %>
	<div class="field">
	<%= f.label :title %><br>
	<%= f.text_field :title %>
	</div>
	
	<div class="field">
	<%= f.label :description %><br>
	<%= f.text_area :description %>
	</div>

	 <%= f.submit "Create Episode", class: "button button_highlight button_block"  %>

	<% end %>